#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"

BIN="$ROOT/build/llog"

if [ ! -x "$BIN" ] && [ ! -f "$BIN" ]; then
  echo "[llog] binary not found; building JVM wrapper..." >&2
  bash "$ROOT/scripts/build-jvm.sh"
fi

exec "$BIN" "$@"
